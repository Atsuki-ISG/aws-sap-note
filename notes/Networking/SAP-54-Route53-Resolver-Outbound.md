---
id: "SAP-54"
title: "Route 53 Resolver Outbound Endpoint"
category: "Networking"
domain: "Design for Organizational Complexity"
tags:
  - "Route 53 Resolver"
  - "Hybrid DNS"
  - "Outbound Endpoint"
created_at: "2026-02-05"
---

# Route 53 Resolver Outbound Endpoint (SAP-54)

## 1. 状況の読み解き (Context & Requirements)

- **構成図の分析**: VPC (AWS) とオンプレミスが VPN で接続されたハイブリッド構成。
- **必須要件**:
    - **AWS → オンプレミス** の名前解決 (corp.internal) が必要。
    - **権威 DNS はオンプレミス** にのみ存在し、AWS へのゾーン転送/複製は禁止 (Compliance)。
    - 可用性とスケーラビリティ、自動化が必要 (hostsファイル管理はNG)。

## 2. 勝負を決めるキーワード (Trigger Words)

- **"VPC 内インスタンスが corp.internal (オンプレ) のホスト名を解決"**:  AWS から オンプレへの方向 = **Outbound**。
- **"権威 DNS を...複製しない"**: Route 53 Private Hosted Zone にコピーしたりセカンダリDNSにするのはNG。Resolver での転送 (Forwarding) 一択。

## 3. なぜその答えか (Reasoning)

- **論理的根拠**: 正解がなぜ '最もスコアが高い' といえるのか、AWSの設計原則に基づいて論理的に解説。
    - Route 53 Resolver **Outbound Endpoint** は、VPC 内からの DNS クエリをオンプレミス等の外部 DNS サーバーに条件付きで転送するための機能です。`corp.internal` ドメインへのクエリのみを VPN 経由でオンプレミスの権威 DNS に投げる「転送ルール (Forwarding Rule)」を作成することで、ゾーン情報を複製せずに要件を満たせます。これはマネージドサービスであり可用性とスケーラビリティも確保されます。
- **選択肢の分析**:
    - **B (hostsファイル)**: "運用負荷大"。数千台規模になった際に破綻するため、"スケーラビリティ" と "自動化" の要件に反するアンチパターン。
    - **C (NSレコード)**: これは「サブドメインの委任」であり、親ゾーン(オンプレ)から子ゾーン(AWS)への委任なら成立するが、今回はオンプレのゾーンそのものを解決したいので不適切。また、空のゾーンを作っても解決できない。
    - **D (Inbound Endpoint)**: これは **オンプレミス → AWS** の名前解決（AWS側の Private Hosted Zone をオンプレから引く）ためのもの。方向が逆。

## 4. 比較と公式資料 (Comparison)

- **比較表**:

| Feature | Direction | Use Case |
| :--- | :--- | :--- |
| **Outbound Endpoint** | AWS ➔ On-Prem | AWSからオンプレのドメインを解決したい時 |
| **Inbound Endpoint** | On-Prem ➔ AWS | オンプレからAWSのPrivate Hosted Zoneを解決したい時 |

- **詳細資料**: [Amazon Route 53 Resolver によるハイブリッドクラウド DNS の解決](https://docs.aws.amazon.com/ja_jp/Route53/latest/DeveloperGuide/resolver.html)

## 5. 学習メモ (Exam Note)

■ AWS SAP Exam Note: Route 53 Hybrid DNS (SAP-54)

> ### 📌 Core Concept
> ハイブリッド環境のDNS解決は方向で使い分ける。
> - AWS **から** オンプレ = **Outbound** Endpoint (Forwarding Rule)
> - オンプレ **から** AWS = **Inbound** Endpoint

> ### 🛠 Trigger ➔ Best Practice
> | 注目要件 (Trigger) | 採るべき構成 (Best Practice) |
> | :--- | :--- |
> | AWS to On-Prem (社内ドメイン) | R53 Resolver **Outbound** Endpoint + 転送ルール |
> | On-Prem to AWS (Private Zone) | R53 Resolver **Inbound** Endpoint + オンプレ側でForward設定 |
> | "一元管理" / "複製しない" | 条件付き転送 (Forwarding) を選択 (ゾーン転送ではない) |

> ### ⚠️ Pitfall / Anti-Pattern
> - hosts ファイルの管理 (SSM等) は、インスタンスが増えると管理不能になるため、SAP試験では常に **不正解**。
> - Inbound と Outbound の逆選択に注意。

> ### 💡 Exam Shortcut
> "AWS **から** 外へ" = **Out**bound
> "外 **から** AWS(In)へ" = **In**bound
